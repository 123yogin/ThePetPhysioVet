# Vercel's Python runtime installs this file for api/index.py (there is no
# api/requirements.txt). The Dockerfile installs backend/requirements.txt.
#
# Both were byte-identical copies with nothing keeping them in sync, so a
# version bump in one would silently give the serverless function and the
# container different dependencies. backend/requirements.txt is the real list,
# because that is the one the Dockerfile COPYs on its own; this includes it.
-r backend/requirements.txt
